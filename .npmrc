# The runtime is pinned to Node 26 in .nvmrc, the Dockerfile and CI. Without
# this, `engines.node` is advisory: npm prints a warning and installs anyway, so
# a machine on an older Node gets a tree that only fails later, somewhere less
# obvious than the install that caused it. Fail at `npm install` instead.
engine-strict=true
